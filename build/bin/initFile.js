const path = require('path');
const resolve = p => path.resolve(__dirname, '../../', p);
const fs = require('fs');

// var result = fs.statSync(resolve('./components.json'));
// console.log('result', result);
